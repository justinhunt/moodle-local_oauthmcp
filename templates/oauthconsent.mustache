{{!
    This file is part of Moodle - http://moodle.org/

    Moodle is free software: you can redistribute it and/or modify
    it under the terms of the GNU General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    Moodle is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU General Public License for more details.

    You should have received a copy of the GNU General Public License
    along with Moodle.  If not, see <http://www.gnu.org/licenses/>.
}}
{{!
    @template local_oauthmcp/oauthconsent

    OAuth consent screen shown at oauth_authorize.php. clientname/clienturi come from a
    DCR registration or a fetched CIMD document, so they are attacker-influenced -
    Mustache's default double-brace escaping (not triple-brace) is relied on throughout.
    consentbody is already a fully rendered, escaped string built in PHP (it embeds
    clientname and the resolved resource's description), so it is safe to output directly.

    Context variables required for this template:
    * clientname string
    * clienturi string|null
    * consentbody string
    * formurl string
    * sesskey string
    * response_type, client_id, redirect_uri, code_challenge, code_challenge_method string
    * state string|null
    * resource string|null
    * scope string|null

    Example context (json):
    {
        "clientname": "Claude",
        "clienturi": "https://claude.ai",
        "consentbody": "Claude wants to access this site's AI tools on your behalf.",
        "formurl": "https://example.com/local/oauthmcp/oauth_authorize.php",
        "sesskey": "abc123",
        "response_type": "code",
        "client_id": "oamc_xyz",
        "redirect_uri": "https://claude.ai/api/mcp/auth_callback",
        "code_challenge": "abc",
        "code_challenge_method": "S256",
        "state": "xyz",
        "resource": null,
        "scope": null
    }
}}
<div class="local_oauthmcp_oauthconsent">
    <h3>{{#str}}oauthconsentheading, local_oauthmcp{{/str}}</h3>
    <p>{{consentbody}}</p>
    {{#clienturi}}
        <p><a href="{{clienturi}}" target="_blank" rel="noopener noreferrer">{{#str}}oauthconsentclientlink, local_oauthmcp{{/str}}</a></p>
    {{/clienturi}}
    <form method="post" action="{{formurl}}">
        <input type="hidden" name="sesskey" value="{{sesskey}}">
        <input type="hidden" name="response_type" value="{{response_type}}">
        <input type="hidden" name="client_id" value="{{client_id}}">
        <input type="hidden" name="redirect_uri" value="{{redirect_uri}}">
        <input type="hidden" name="code_challenge" value="{{code_challenge}}">
        <input type="hidden" name="code_challenge_method" value="{{code_challenge_method}}">
        {{#state}}<input type="hidden" name="state" value="{{state}}">{{/state}}
        {{#resource}}<input type="hidden" name="resource" value="{{resource}}">{{/resource}}
        {{#scope}}<input type="hidden" name="scope" value="{{scope}}">{{/scope}}
        <button type="submit" name="confirm" value="approve" class="btn btn-primary">{{#str}}oauthconsentapprove, local_oauthmcp{{/str}}</button>
        <button type="submit" name="confirm" value="deny" class="btn btn-secondary">{{#str}}oauthconsentdeny, local_oauthmcp{{/str}}</button>
    </form>
</div>
